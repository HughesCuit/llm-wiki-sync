# Hermes self-learn auto-sync integration
#
# Add git push to ~/.hermes/hooks/self-learn/handler.py
# After _write_learning_wiki() returns True, add:
#
import subprocess, os
os.chdir(os.path.expanduser("~/wiki"))
subprocess.run(["git", "add", "-A"], capture_output=True)
r = subprocess.run(["git", "commit", "-m", f"self-learn: {tag} {pattern_key}"], capture_output=True)
if r.returncode == 0:
    subprocess.run(["git", "push"], capture_output=True)
